# Copy to `.env` and fill in for your local setup.
# `.env` itself is git-ignored.

# Path to the Ableton Live application that hosts the extension.
# `extensions-cli run` reads this to know which Live build to launch.
EXTENSION_HOST_PATH=/Applications/Ableton Live 12 Beta.app
